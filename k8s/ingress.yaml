apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ragagument-ingress
  namespace: ragagument-production
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "X-Content-Type-Options: nosniff";
      more_set_headers "X-Frame-Options: DENY";
      more_set_headers "X-XSS-Protection: 1; mode=block";
spec:
  tls:
  - hosts:
    - ragagument.example.com
    secretName: ragagument-tls
  rules:
  - host: ragagument.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ragagument-service
            port:
              number: 8501
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ragagument-ingress-staging
  namespace: ragagument-staging
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
spec:
  tls:
  - hosts:
    - staging.ragagument.example.com
    secretName: ragagument-staging-tls
  rules:
  - host: staging.ragagument.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ragagument-service-staging
            port:
              number: 8501
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ragagument-ingress-dev
  namespace: ragagument-development
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
spec:
  rules:
  - host: dev.ragagument.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ragagument-service-dev
            port:
              number: 8501